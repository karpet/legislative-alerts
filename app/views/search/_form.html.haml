= panel title: 'Bill Search', context: :primary do
  = simple_form_for(:search, url: search_path,
      html: { method: 'get', class: 'form-horizontal' },
      wrapper: :horizontal_form ) do |f|

    .panel-body
      = f.error_notification

      .form-inputs
        = f.input :q, autofocus: true, label: 'Full text', required: false,
            input_html: { value: @query[:q] }
        = f.input :state, collection: us_states,
            label: 'Limit to State', required: false,
            selected: @query[:state],
            include_blank: true,
            input_html: { style: 'width:auto' }

    .panel-footer
      .buttons
        = f.button :submit, 'Search', class: "btn-primary"
        = link_to 'Cancel', search_path, class: 'btn btn-secondary'
