#!/bin/sh

git pull && \
bin/cron-bundle install && \
bin/cron-bundle rake db:migrate && \
bin/cron-bundle rake assets:precompile && \
bin/cron-bundle rake restart && \
bin/cron-bundle exec bin/delayed_job restart
